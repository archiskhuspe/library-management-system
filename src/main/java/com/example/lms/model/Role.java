package com.example.lms.model;

/**
 * Defines the roles a user can have within the system.
 */
public enum Role {
    ROLE_USER,  // Standard user, typically a library member
    ROLE_LIBRARIAN // A librarian with administrative privileges
} 